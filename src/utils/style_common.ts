import { mainColor, whiteColor } from "./app_color";

export const ButtonStyle =() => {
    var style: any = {
        backgroundColor: mainColor,
        borderColor: mainColor,
        color: whiteColor,
        borderRadius: '4px',
    }

    return style;
}